/**
 * 空状態のマイクロコピー。
 *
 * 全画面の `EmptyState` は「アイコン + 状況説明 + 次の一手」で作り込まれている一方、
 * カード内・インラインの空表示は `(説明なし)` のような括弧書きで放置されており、
 * 同一アプリ内で空状態の品質にムラがあった。文言をここに集約してトーンを揃える。
 *
 * 書き方の原則:
 * 1. 括弧書き `( )` を使わない(システムの null 表示に見える)
 * 2. 否定形で終わらせず、次の一手を添える
 * 3. 丁寧語で統一する(全画面版と同じトーン)
 *
 * `canWrite` / `readOnly` の 2 本を持つものは、書き込み権限が無い閲覧者に
 * 実行できない操作を促さないため。CTA に出す文言は実際のボタンラベルと一致させること
 * (現状: `編集` / `AI で生成` / `AI で一括生成`)。
 */
export const EMPTY_MESSAGES = {
  /** プロジェクト詳細の「概要」セクション。 */
  projectDescription: {
    canWrite: '概要はまだありません。「編集」から追加できます。',
    readOnly: '概要はまだ設定されていません。',
  },
  /** プロジェクト詳細の README プレビュー / README ページ本体。 */
  readme: {
    canWrite: 'README はまだありません。「AI で生成」で下書きを作れます。',
    readOnly: 'README はまだ作成されていません。',
  },
  /** README の本文が空(タイトルだけ存在する版)。 */
  readmeBody: {
    canWrite: '本文が空です。「編集」から書き足せます。',
    readOnly: '本文はまだ書かれていません。',
  },
  /** チェックリストのカテゴリ内に項目が無い。 */
  checklistCategory: {
    canWrite: '項目がありません。下の欄から追加するか、「AI で一括生成」で提案を受けられます。',
    readOnly: 'このカテゴリにはまだ項目がありません。',
  },
  /** 告知カードの配信文面が未生成。生成は告知の詳細ページで行う。 */
  announcementDeliveries: '配信文面はまだありません。開いて AI で生成できます。',
} as const;
