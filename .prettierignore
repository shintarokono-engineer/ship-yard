# Dependencies
node_modules
.pnpm-store

# Build outputs
dist
build
.next
.turbo

# Lock files
pnpm-lock.yaml

# Claude Code outputs
.claude/output

# 自動生成ファイル(整形は生成元ツールに委ねる)
**/*.generated.md
packages/db/prisma/migrations

# Terraform の作業ディレクトリ(プロバイダのバイナリを読もうとして exit 2 になる)
**/.terraform
**/*.tfstate
**/*.tfstate.backup

# Claude Code の作業領域(worktree は同じリポジトリの別チェックアウト)
.claude
